# Core — works on Python 3.9+
requests>=2.32.0
python-dotenv>=1.0.1
pyyaml>=6.0.2
rich>=13.9.0

# Agent-via-MCP path (sweep_mcp.py) — requires Python 3.10+
# Drop this line on Python 3.9; the HTTP path (sweep.py, run.py) still works.
mcp>=1.0.0; python_version >= "3.10"
